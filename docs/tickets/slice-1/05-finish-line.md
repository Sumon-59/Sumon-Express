# 05 — Finish line: review → merge → deploy

**What to build:** The slice's definition of done: full backend + frontend suites, type
checks, builds, and the compiled-artifact smoke check green; two-axis `/code-review` on
the branch with findings fixed; merge to the deploy branch; tracker updated; live site
verified after deploy (login + checkout still work; admin shell reachable for the
promoted admin).

**Blocked by:** 04 — Admin shell.

**Status:** ready-for-agent

- [x] All suites, type checks, builds, smoke green
- [x] /code-review findings addressed
- [x] Merged to fullstack-v2; plan.md tracker updated; deploy verified
