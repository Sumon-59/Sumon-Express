# 04 — Finish line: review, merge, deploy, verify, document

**What to build:** The slice reviewed, merged, live, and documented. Two-axis review
with findings fixed or explicitly accepted; merge `--no-ff` to `fullstack-v2`, push as
Sumon-59; probes — the preview endpoint 404 → 401 in production, then a real code
created, previewed, and spent against production via authenticated API calls (the
end-to-end proof, per the full-autonomy contract folding demos into probes). CLAUDE.md
gains the discounts section; spec + tickets ticked; plan.md tracker updated.

**Blocked by:** 03.

**Status:** ready-for-agent

- [ ] Two-axis code review run; findings fixed or explicitly accepted
- [ ] Merged `--no-ff` to `fullstack-v2`, pushed as Sumon-59
- [ ] Probe: production preview endpoint 404 → 401 without auth
- [ ] Probe: real code created + previewed against production (authenticated API)
- [ ] CLAUDE.md updated; spec + ticket checkboxes ticked; plan.md tracker updated
